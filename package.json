{
  "name": "customized-gradebook",
  "version": "1.0.2",
  "type": "module",
  "scripts": {
    "export:all": "node buildScripts/export-all-mermaid.js",
    "test": "vitest",
    "deploy:dev": "npm run build:dev && gh release upload dev dist/dev/customGradebookInit.js dist/dev/customGradebookInit.js.map --clobber",
    "release:patch": "npm version patch && git push --follow-tags && npm run deploy:prod",
    "release:minor": "npm version minor && git push --follow-tags && npm run deploy:prod",
    "release:major": "npm version major && git push --follow-tags && npm run deploy:prod"
  },
  "devDependencies": {
    "esbuild": "^0.27.0",
    "jsdom": "^27.3.0",
    "vitest": "^4.0.15"
  },
  "dependencies": {
    "@mermaid-js/mermaid-cli": "^11.12.0"
  }
}